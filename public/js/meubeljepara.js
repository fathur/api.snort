var windowHeight 	= $(window).height();
var windowWidth		= $(window).width();

console.log(windowHeight);

/*$(document).ready(function() {
	$('.main-nav-collapse').removeAttr('height').css('height', windowHeight);
});*/
